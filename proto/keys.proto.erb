syntax = "proto2";
package msgs;

<%
require 'yaml'

table = YAML.load STDIN
%>

enum Keycode {
    KeyUnknown = 0;

    <%- table.each_with_index do |e, index| -%>
    <%= e['proto'] %> = <%= index + 1 %>;
    <%- end -%>
}
